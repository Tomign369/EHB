let login = document.querySelector("#sign-up"); // Use # for ID
let loginContainer = document.querySelector(".login-container");

login.addEventListener("click", function () {
  loginContainer.style.visibility = "visible"; // Fixed typo
});
